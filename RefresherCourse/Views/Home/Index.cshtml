@model Index
@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-md-12">
        <h2>@Model.Title</h2>
        <p>
            @Model.Content
        </p>
    </div>
    <div class="col-md-12">
        @Html.Action("Slider")
    </div>
</div>
